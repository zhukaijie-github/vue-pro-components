'use strict';

const vueProComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(vueProComponents(), 'Hello from vueProComponents');
console.info('vueProComponents tests passed');
